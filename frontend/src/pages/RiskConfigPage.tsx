import { PlaceholderPanel } from "../components/common/PlaceholderPanel";

export function RiskConfigPage() {
  return (
    <PlaceholderPanel
      description="Màn hình này giữ bố cục của design để demo. CRUD threshold và lưu backend sẽ được nối sau."
      title="Cấu hình ngưỡng rủi ro"
    />
  );
}
