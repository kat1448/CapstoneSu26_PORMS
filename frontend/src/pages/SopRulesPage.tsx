import { PlaceholderPanel } from "../components/common/PlaceholderPanel";

export function SopRulesPage() {
  return (
    <PlaceholderPanel
      description="Route đã sẵn sàng cho danh sách và chỉnh sửa SOP. Hiện tại giữ layout phục vụ demo giao diện."
      title="SOP Rules"
    />
  );
}
